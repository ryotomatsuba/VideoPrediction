
output_dir: Unet
mlrun_path: /home/data/ryoto/Unet/mlruns/
experiment:
  name: Mnist
dataset: 
  num_data: 1000
  input_num: 4
# Training configs
train:
  epochs: 100
  batch_size: 8
  save_best_ckpt: True
  num_workers: 2
  ckpt_path: best_ckpt.pth
  lr: 0.0001
  val_percent: 0.1 # Percent of the data that is used as validation (0-1)

# Model configs
model:
  load: False # Load model from a .pth file

scale: 0.5 # Downscaling factor of the images